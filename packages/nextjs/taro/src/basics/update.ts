import {unsupported} from '../utils'

/**
 * 更新客户端版本。当判断用户小程序所在客户端版本过低时，可使用该接口跳转到更新微信页面。
 */
export const updateWeChatApp = unsupported._void('updateWeChatApp')

/**
 * 获取全局唯一的版本更新管理器，用于管理小程序更新。关于小程序的更新机制，可以查看运行机制文档。
 */
export const getUpdateManager = unsupported._void('getUpdateManager')
