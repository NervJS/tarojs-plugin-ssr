import {unsupported} from '../utils'

/**
 * 小程序测速上报。使用前，需要在小程序管理后台配置。
 */
export const reportPerformance = unsupported._void('reportPerformance')

/**
 * 获取当前小程序性能相关的信息。
 */
export const getPerformance = unsupported._void('getPerformance')
