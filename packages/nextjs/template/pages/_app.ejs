import Head from 'next/head'
import {
    initTaroApp,
    initPxTransform,
    registerToastComponent,
    registerPreviewComponent
} from 'tarojs-plugin-platform-nextjs/taro'
import {Toast, Preview} from 'tarojs-plugin-platform-nextjs/components'
import TaroApp from '../src/app'
import customRoutes from '../customRoutes.json'
import 'tarojs-plugin-platform-nextjs/components/dist/lilin.css'

initTaroApp(customRoutes)
initPxTransform({designWidth: <%- designWidth %>})
registerToastComponent(Toast)
registerPreviewComponent(Preview)

const MyApp = ({Component, pageProps}) => (
    <>
        <Head>
            <meta
                name="viewport"
                content="width=device-width,initial-scale=1,user-scalable=no,viewport-fit=cover"
            />
        </Head>
        <TaroApp>
            <Component {...pageProps} />
        </TaroApp>
    </>
)

export default MyApp
